version: "3"
services:
    frontend:
        build:
            context: ../frontend
            dockerfile: Dockerfile
        volumes:
            - "../frontend:/app"
        ports:
            - "4200:4200"
